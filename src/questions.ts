import type { Question } from './types';

// This file is now only used for types, as questions are generated dynamically
export const initialQuestion: Question = {
  text: "Comment te sens-tu aujourd'hui ?",
  type: "text",
  allowImage: true
};
